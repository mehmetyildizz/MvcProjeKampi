@model EntityLayer.Concrete.Category
@{
    ViewBag.Title = "KategoriDuzenle";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Kategoriler</h1>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>
<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <!-- left column -->
            <div class="col-md-12">
                <!-- jquery validation -->
                <div class="card card-primary">
                    <div class="card-header">
                        <h3 class="card-title">Kategori Düzenle</h3>
                    </div>
                    <!-- /.card-header -->
                    <!-- form start -->
                    @using (Html.BeginForm("KategoriDuzenle", "Category", FormMethod.Post, new { @id = "quickForm" }))
                    {
                        <div class="card-body">
                            <div class="form-group">
                                <label for="txtKategoriId">Kategori Adı</label>
                                @Html.TextBoxFor(x => x.CategoryID, new { @readonly = "readonly", @class = "form-control", @id = "txtKategoriId" })
                            </div>
                            <div class="form-group">
                                <label for="txtKategoriAdi">Kategori Adı</label>
                                @Html.TextBoxFor(x => x.CategoryName, new { @placeholder = "Kategori Adı Giriniz", @class = "form-control", @id = "txtKategoriAdi" })
                                @Html.ValidationMessageFor(x => x.CategoryName, string.Empty, new { @class = "error invalid-feedback d-block", @id = "txtKategoriAdi-error" })
                            </div>
                            <div class="form-group">
                                <label for="txtKategoriAciklama">Kategori Açıklama</label>
                                @Html.TextAreaFor(x => x.CategoryDescription, new { @placeholder = "Kategori Açıklaması Giriniz", @class = "form-control", @id = "txtKategoriAciklama" })
                                @Html.ValidationMessageFor(x => x.CategoryDescription, string.Empty, new { @class = "error invalid-feedback d-block", @id = "txtKategoriAciklama-error" })
                            </div>
                            <div class="form-group">
                                <label for="exampleCheck1">Kategori Durumu</label><br />
                                <style>
                                    .toggle.android label {
                                        font-weight: 400;
                                    }
                                </style>
                                @Html.CheckBoxFor(x => x.CategoryStatus, new { id = "exampleCheck1", data_toggle = "toggle", data_style = "android", data_on = "Kategori Aktif", data_off = "Kategori Pasif", data_onstyle = "success", data_offstyle = "danger", data_height = "38", data_width = "150" })
                            </div>
                        </div>
                        <!-- /.card-body -->
                        <div class="card-footer">
                            <button type="submit" class="btn btn-primary">Değişiklikleri Kaydet</button>
                        </div>
                    }
                </div>
                <!-- /.card -->
            </div>
            <!--/.col (left) -->
            <!-- right column -->
            <div class="col-md-6">

            </div>
            <!--/.col (right) -->
        </div>
        <!-- /.row -->
    </div><!-- /.container-fluid -->
</section>
<!-- /.content -->